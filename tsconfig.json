{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    "types": ["node"]
  },
  "include": ["*.ts", "scripts/**/*.ts"],
  "exclude": ["node_modules", ".turbo", "dist", "build", "out", "coverage", "apps", "packages"]
}
